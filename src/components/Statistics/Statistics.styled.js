import { styled } from 'styled-components';

export const StatBlock = styled('div')(() => {
  return {
    fontWeight: 500,
    fontSize: 20,
    lineHeight: 1.5,
    letterSpacing: '0.02em',
    color: '#2E2F42',
  };
});
